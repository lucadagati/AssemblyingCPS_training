# Replaces the stock _6040_iot_webservices_panel.py.
# The empty Web Services panel is superseded by iot_wot.
# PANEL is required so Horizon processes this file,
# but ADD_PANEL is omitted so nothing gets registered.
PANEL = 'webservices'
PANEL_DASHBOARD = 'iot'
PANEL_GROUP = 'iot'
DEFAULT_PANEL = ''
