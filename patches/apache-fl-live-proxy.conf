# Federated Learning live dashboard — reverse proxy for Horizon iframe (same origin)
<IfModule mod_proxy.c>
  ProxyPreserveHost On
  ProxyPass /horizon/fl-live/ http://host.docker.internal:8090/
  ProxyPassReverse /horizon/fl-live/ http://host.docker.internal:8090/
</IfModule>
